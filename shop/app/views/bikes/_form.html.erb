<%= form_with(model: bike, local: true) do |form| %>
  <% if bike.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bike.errors.count, "error") %> prohibited this bike from being saved:</h2>

      <ul>
      <% bike.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :make %>
    <%= form.text_field :make, id: :bike_make %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :bike_color %>
  </div>

  <div class="field">
    <%= form.label :used_for %>
    <%= form.text_field :used_for, id: :bike_used_for %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :bike_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
